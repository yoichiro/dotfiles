# Local-only files (never commit)
*.local
zshrc.local
bashrc.local
profile.local

# But these are part of the repo (loader pattern overrides upstream defaults)
!zprezto/zpreztorc.local

# Backups generated by install.sh
*.bak

# Editor / OS junk
*.swp
*.swo
.DS_Store
Thumbs.db

# Claude Code local settings
.claude/settings.local.json
